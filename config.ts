export const BASE_URLS = {
  // Use internal Next.js API routes (no trailing slash for RTK Query compatibility)
  production: "/api",
  local: "/api",
  // Old external backend (for reference)
  // external: "https://global-vin-bacckend.vercel.app/api/",
};
